/*
 Theme Name: {{basicInfo.websiteName}}
 Author: Theme Generator
 Version: 1.0
*/
body {
  background-color: {{designPreferences.brandColors}};
  font-family: {{designPreferences.fontPairing}};
}

/* Feature-specific styles */
{{#each selectedFeatures}}
{{#if this}}
.{{this}} {
  display: block;
  padding: 20px;
}
{{/if}}
{{/each}}

header {
  {{layoutStructure.headerStyle}};
}

footer {
  {{layoutStructure.footerStyle}};
}

/* Animation styles */
{{#if layoutStructure.animationStyle}}
.animated {
  animation: {{layoutStructure.animationStyle}};
}
{{/if}}
