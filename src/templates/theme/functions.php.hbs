
<?php
function {{basicInfo.websiteName}}_theme_setup() {
  add_theme_support('title-tag');
  {{#if selectedFeatures}}
  register_nav_menus(array(
    'primary' => __('Primary Menu', '{{basicInfo.websiteName}}'),
  ));
  {{/if}}
  add_theme_support('post-thumbnails');
}
add_action('after_setup_theme', '{{basicInfo.websiteName}}_theme_setup');
?>

